<p>user index</p>

<h1>Users</h1>

<thead>
  <tr>
    <th>image</th>
    <th>name</th>
    <th></th>
  </tr>
</thead>
<% @users.each do |user| %>
<tbody>
  <tr>
    <td><%= user.name %></td>
    <td></td>
    <td><%=link_to"show", user_path(user.id) %></td>
  </tr>
</tbody>
<% end %>